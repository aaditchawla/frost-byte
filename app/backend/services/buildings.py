# Building feature extraction (Overpass)
async def get_building_features(lat: float, lon: float):
    """
    note: this is a temporary stub
    it returns placeholder building features
    """
    return {
        "building_count_40m": 0,
        "building_area_m2_40m": None,
        "source": "stub"
    }
